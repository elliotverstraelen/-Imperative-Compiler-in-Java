package compiler.CodeGenerator;
import compiler.Parser.Program;
import compiler.Exceptions.CodeGenerationException;
public class CodeGenerator {
    public static byte[] generateByteCode(Program program) throws CodeGenerationException {
        // Logic to generate code from a Program object here
        // For now just return an empty array
        return new byte[0];
    }
    public static String generateCode(Program program) throws CodeGenerationException {
        // Logic to generate code from a Program object here
        // For now just return an empty string
        return "";
    }
}
