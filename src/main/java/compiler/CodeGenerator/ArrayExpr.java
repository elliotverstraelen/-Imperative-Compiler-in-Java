package compiler.CodeGenerator;

import org.objectweb.asm.MethodVisitor;

public class ArrayExpr extends Expression {
    // add relevant fields

    @Override
    public void generateCode(MethodVisitor visitor) {
        // logic to generate bytecode for array expression
    }
}
